// criando uma array de 5 numero

const numeros = [7, 10, 11, 25, 77,];

let i = 0;
//enquanto i for menor que o tamanho do array
while(i < numeros.length) {
    //executar isso
    console.log(numeros[i]);
    i++;
}